services:
  loom:
    build: .
    ports:
      - "4444:4444"
    volumes:
      - ./loom.yaml:/app/loom.yaml:ro
      - ./data:/app/data
    environment:
      LOOM_CONFIG: /app/loom.yaml
    restart: unless-stopped
