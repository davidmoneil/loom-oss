services:
  loom:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./loom.yaml:/app/loom.yaml:ro
      - ./data:/app/data
    environment:
      LOOM_CONFIG: /app/loom.yaml
    restart: unless-stopped
