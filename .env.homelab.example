# Copy to .env.homelab and fill in — this is the only secret the homelab
# deployment needs (loom.homelab.yaml is tracked and secret-free).
LOOM_POSTGRES_DSN=postgresql://user:password@host.docker.internal:5432/loom
